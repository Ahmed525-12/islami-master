import 'package:flutter/material.dart';

class HadethPage extends StatelessWidget {
  const HadethPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
