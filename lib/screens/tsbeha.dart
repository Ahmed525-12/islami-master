import 'package:flutter/material.dart';

class TsbehaPage extends StatelessWidget {
  const TsbehaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
